package ru.tinkoff.client.rest;

import io.netty.channel.ChannelOption;
import org.springframework.web.reactive.function.client.WebClient;
import reactor.netty.tcp.TcpClient;

public class RestClient {
   /* private static final TcpClient timeoutClient = TcpClient
            .create()
            .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, 100 * 10)
            .
    private static final WebClient client = WebClient.builder().clientConnector()*/
}
